export enum HTTP_METHODS {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  PATCH = "PATCH",
  DELETE = "DELETE",
}

export enum HTTP_STATUS_CODE {
  success = 200,
  created = 201,
  error = 500,
}

export enum HTML_TAGS {
  text = "text",
  body = "body",
  div = "div",
  span = "span",
  button = "button",
  a = "a",
  input = "input",
  textarea = "textarea",
  p = "p",
  label = "label",
}

export enum PAGES {
  PIM = "PIM",
}

export enum COMMON_BUTTONS {
  ADD = "Add",
  SAVE = "Save",
}
